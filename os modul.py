import os
#os.mkdir("lokesh") #make a dir
#os.chdir("lokesh")  #creat dir

#f.write("hello.txt","w")
#f.write("hello world")
#f.close()
#os.remove("hello.txt")   #remov file
#os.rmdir("your.txt")
#print(os.getcwd())     #show file in folder 
#print(os.listdir("c:"))  #driv in all folder
#print(os.rmdir("hello")) #remov this folder
#print(os.listdir())     #py all file
#location='d:/dbms'    #file location
#for file in os.listdir(location):
 #   print(os.path.join(location,file))
 #totel number of folder are avelible
#print("the totel number of folder are avelible in:-")
#print(len(os.listdir("c:/")))
'''from  pathlib import path
def endswith1(s):
    if s.sindex(':')>=0:
        return s[s.index(':'):]
    else:
        return s
    imgcount=0
    txtcount=0
    entries=path('d:/html')
    for entry in entries.iterdir():
        print(entry1.name)
    try:
        if endwith1(entry1.name)=='png'
        or endwith1(entry1.name)=="jpg"
        or endwith1(entry1.name)=="gif":
                imgcount=imgcount+1
    elif
        endwith1(entry1.name)=="png"
        or endwith1(entry1.name)=="jpg"
        or endwith1(entry1.name)=="gif":
            txtcount=txtcount+1
    except:

            pass
                
    else:
        if endwith1(entry1.name)=="png"
        or endwith1(entry1.name)=="jpg"
        or endwith1(entry1.name)=="gif":
            imgcount=imgcount+1
        elif endwith1(entry.name)=="txt"
            or
              endwith1(entry.name)=="doc":
               imgcount=imgcount+1
            print("total image are "+str(imgcount))
            print("total txt file are "str(txtcount))'''
os.path.exists
path=input("enter the path\n")
isexist=os.path.exists(path)
print(isexist)
    imgcount=0
    vidcount=0
    txtcount=0
    for file in files:
        if endwith1(entry1.name)==('.jpj')or
         endwith1(entry1.name)==("jpeg")or
         endwith1(entry1.name)==("png"):
                imgcount=imgcount+1
        elif endwith1(entry1.name)==('mk')or
        or endwith1(entry1.name)=="jpg"
        or endwith1(entry1.name)=="gif":
                imgcount=imgcount+1








        
